/* Platform - Codeforces
* Username - rootn
* ProblemName or Id -
*/

#include <bits/stdc++.h>
using namespace std;

#define INF 0x3f3f3f3f
typedef long long ll;
typedef vector<int> vi;

int main(void)
{
    int s, b;
    cin >> s >> b;
    while(s && b)
    {
        vector<bool> v(s+1, true);
        while(b--)
        {
            int p, q; cin >> p >> q;
            
        }

        cout << "-" << endl;
        cin >> s >> b;
    }
}
